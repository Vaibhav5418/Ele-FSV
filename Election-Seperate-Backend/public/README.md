Place the Jessibuca player bundle here as:

`jessibuca.js`

The backend endpoint `GET /election/api/fsv/jessibuca.js` will serve this file first.

Optional override:
- Set `JESSICA_LOCAL_SCRIPT_PATH` to an absolute file path if you want to serve from another location.
